About
=====

The gognuflag library provides a programmatically similar interface similar
to the built-in 'flag' package, except the flag and command-line semantics
are identical to those of GNU getopt_long().

It is released under a GPLv3+ license.

Building
========

Just run 'make'.
